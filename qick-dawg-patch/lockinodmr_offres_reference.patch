--- qick-dawg (backup)/src/qickdawg/nvpulsing/lockinodmr.py	2026-04-07 10:03:57
+++ qick-dawg/src/qickdawg/nvpulsing/lockinodmr.py	2026-04-20 15:42:22
@@ -184,14 +184,9 @@
             t=self.cfg.readout_integration_treg)
 
         t_ref = self.cfg.readout_integration_treg + self.cfg.relax_delay_treg
-        if getattr(self.cfg, "odmr_reference_zero_gain_pulse", False):
-            ref_gain = int(getattr(self.cfg, "odmr_reference_pulse_gain", 1))
-            self.set_pulse_registers(
-                ch=self.cfg.mw_channel,
-                style='const',
-                gain=ref_gain,
-                length=self.cfg.readout_integration_treg,
-                phase=0)
+        ref_offres_mhz = getattr(self.cfg, "odmr_reference_offres_mhz", None)
+        if ref_offres_mhz is not None:
+            self.mw_frequency_register.set_to(float(ref_offres_mhz))
             self.pulse(ch=self.cfg.mw_channel, t=t_ref)
 
         self.trigger(
